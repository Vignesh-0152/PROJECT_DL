from tensorflow.keras.callbacks import TensorBoard, EarlyStopping, CSVLogger

class callbacks :
    def __init__(self, log_dir="./logs"):
        pass

    def call():
        tensorboard = TensorBoard(

        )